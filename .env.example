# Siri Bridge Configuration
# Copy this file to /etc/siri-bridge.env and fill in your values
# Then run: sudo chmod 600 /etc/siri-bridge.env

# Required: Your secret API key (generate with: openssl rand -hex 32)
API_KEY=your-secret-key-here

# Optional: Port to listen on (default: 18790)
PORT=18790

# Optional: Host to bind to (default: 127.0.0.1)
# Use 127.0.0.1 for security - expose via reverse proxy only
BIND_HOST=127.0.0.1

# Optional: OpenClaw agent ID (default: main)
AGENT=main

# Optional: Agent timeout in seconds (default: 120)
TIMEOUT=120

# Optional: Rate limiting - max requests per window (default: 10)
RATE_LIMIT_MAX=10

# Optional: Rate limit window in milliseconds (default: 60000 = 1 minute)
RATE_LIMIT_WINDOW_MS=60000

# Optional: Maximum message length in characters (default: 4000)
MAX_MESSAGE_LENGTH=4000

# Optional: CLI command to use (default: clawdbot)
# Use 'openclaw' if you have OpenClaw installed instead of Clawdbot
CLI_COMMAND=clawdbot
